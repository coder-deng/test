# TechScout TODO List

> 项目场景与范围见 [project-scenario.md](./project-scenario.md)。  
> 当前目标：先完成 Sprint 0 的单 Agent 最小闭环，再根据复盘结果进入后续 Sprint。

## 当前状态

- 当前 Sprint：Sprint 0
- 当前里程碑：命令行输入问题，生成带来源的 Markdown 技术选型报告
- 开发语言：Python 3.11+（已确认）
- 成本约束：除 LLM Token 外，所有组件和服务必须免费
- MVP 架构：已完成，见 `docs/mvp-architecture.md`
- 业务代码状态：尚未开始

## 全局成本检查（每个 Sprint 都必须满足）

- [x] Deep Agents、LangChain 和 LangGraph 使用开源版本
- [x] 搜索使用无需 API Key、无按请求账单的 DDGS
- [x] 报告和运行状态保存在本地文件系统
- [x] 不启用 LangSmith 云端追踪、评估或托管
- [x] 不使用付费数据库、缓存、队列或对象存储
- [x] 不使用依赖有限免费额度的核心服务
- [ ] 每次增加依赖前检查许可证和计费模式
- [ ] 每个 Sprint 验收时确认不存在 LLM 之外的计费路径

## Sprint 0：最小可运行版本

### 0. 开发环境确认

- [x] 确定使用 Python 开发
- [x] 确认本机 Python 版本为 3.11+（当前：Python 3.13.14）
- [x] 确认已安装 `uv`（当前：uv 0.12.0）
- [x] 确定首个模型供应商（默认 OpenAI；具体模型名称待配置）
- [ ] 准备模型 API Key
- [x] 确定免费搜索实现：DDGS（不需要搜索 API Key）
- [x] 确认不启用搜索服务付费账户

### 1. 初始化项目

- [ ] 使用 `uv` 初始化 Python 项目
- [ ] 添加 `deepagents`、模型适配包和搜索依赖
- [ ] 添加 `pytest`、`ruff` 等开发依赖
- [ ] 建立 `src/` 源码目录
- [ ] 建立 `tests/` 测试目录
- [ ] 建立 `workspace/` Agent 工作目录
- [ ] 创建 `.gitignore`
- [ ] 创建 `.env.example`，只保留变量名和说明
- [ ] 确认真实 `.env` 不会被提交
- [ ] 在 `.env.example` 中默认设置 `LANGSMITH_TRACING=false`

### 2. 建立配置层

- [ ] 从环境变量读取模型名称和 API Key
- [x] 搜索工具不依赖外部付费 API Key
- [ ] 配置默认工作目录
- [ ] 缺少必需配置时给出清晰错误信息
- [ ] 避免在日志和异常中输出 API Key

### 3. 实现搜索工具

- [ ] 定义搜索工具的输入参数和说明
- [ ] 使用 DDGS 执行元搜索并限制单次结果数量
- [ ] 将结果整理为标题、摘要、URL 的统一格式
- [ ] 处理超时、空结果、限流和上游搜索引擎异常
- [ ] 为搜索实现预留可替换接口，避免绑定 DDGS
- [ ] 为搜索工具编写单元测试

### 4. 创建 Deep Agent

- [ ] 使用 `create_deep_agent` 创建 Agent
- [ ] 注入搜索工具
- [ ] 配置本地文件系统 Backend
- [ ] 编写最小系统提示词
- [ ] 要求 Agent 先建立调研计划再开始检索
- [ ] 要求 Agent 将报告写到指定工作目录
- [ ] 约束 Agent 不覆盖场景文档和项目源码
- [ ] 限制首版搜索轮次，避免失控调用

### 5. 设计报告格式

- [ ] 确定 topic 目录命名规则
- [ ] 创建 `brief.md`
- [ ] 创建 `report.md`
- [ ] 报告包含“问题与约束”
- [ ] 报告包含“候选方案比较”
- [ ] 报告包含“推荐结论及适用条件”
- [ ] 报告包含“风险与未知项”
- [ ] 报告包含“后续验证建议”
- [ ] 报告包含可访问的信息来源 URL

### 6. 实现 CLI

- [ ] 支持通过命令参数输入调研问题
- [ ] 未提供参数时支持交互式输入
- [ ] 在终端展示 Agent 的关键进度
- [ ] 运行成功后输出报告绝对路径
- [ ] 运行失败时返回非零退出码和可理解的提示
- [ ] 在 README 中记录启动命令和示例

### 7. 测试与质量检查

- [ ] 测试配置读取与缺失配置
- [ ] 测试搜索结果格式化
- [ ] 测试工作目录命名与路径安全
- [ ] 测试报告必需章节校验
- [ ] 使用 Mock 避免单元测试消耗真实 API
- [ ] 运行全部 `pytest`
- [ ] 运行 `ruff check`
- [ ] 运行 `ruff format --check`

### 8. 端到端验收

- [ ] 使用固定问题运行一次完整流程
- [ ] 固定验收问题：`3 人团队开发内部 AI API，FastAPI 和 Django 应该如何选择？`
- [ ] Agent 在执行过程中维护调研计划
- [ ] 成功生成独立 topic 目录
- [ ] 成功生成 `brief.md`
- [ ] 成功生成 `report.md`
- [ ] 报告包含至少 2 个候选方案
- [ ] 报告至少覆盖开发效率、异步能力、生态和维护成本
- [ ] 关键判断带有来源 URL
- [ ] 报告明确说明风险和未知信息
- [ ] 检查运行产物未写入源码或 `docs/` 目录
- [ ] 检查日志未泄露密钥

### 9. Sprint 0 收尾

- [ ] 更新 README 和运行示例
- [ ] 记录实际使用的模型、耗时和搜索次数
- [ ] 记录遇到的问题及解决方式
- [ ] 对照 Definition of Done 完成检查
- [ ] 编写 Sprint 0 Review
- [ ] 编写 Sprint 0 Retrospective
- [ ] 根据反馈调整 Sprint 1 Backlog

## Sprint 1：结构化过程与可靠输出

- [ ] 增加可复用的调研 brief 模板
- [ ] 分开保存各候选方案的研究笔记
- [ ] 保存来源标题、URL、访问时间和摘要
- [ ] 增加来源去重
- [ ] 增加来源数量下限校验
- [ ] 增加报告必需章节校验
- [ ] 增加中断、超时和重试策略
- [ ] 增加结构化运行日志
- [ ] 补充核心流程集成测试
- [ ] 建立报告质量验收清单

## Sprint 2：多代理调研

- [ ] 定义主 Agent 与研究子代理的职责边界
- [ ] 为每个候选方案配置独立研究子代理
- [ ] 主 Agent 负责拆解、委派、综合和最终报告
- [ ] 限制子代理数量和调用次数
- [ ] 处理子代理失败或返回空结果的情况
- [ ] 对比单 Agent 与多 Agent 的报告质量
- [ ] 对比单 Agent 与多 Agent 的耗时及 Token 成本
- [ ] 记录适合和不适合委派的任务类型

## Sprint 3：持久化与人工控制

- [ ] 接入 Checkpointer
- [ ] 为每次调研生成稳定的 thread ID
- [ ] 支持恢复中断任务
- [ ] 支持用户补充约束后继续任务
- [ ] 在高成本检索前增加人工确认
- [ ] 在最终报告写入前增加人工确认
- [ ] 支持批准、修改和拒绝操作
- [ ] 记录运行轨迹和基础指标

## Sprint 4：Skills 与长期偏好（可选）

- [ ] 抽取“技术选型调研” Skill
- [ ] 把报告规范从系统提示词移入 Skill
- [ ] 增加不同类型的报告模板
- [ ] 保存用户常用的比较维度
- [ ] 保存用户对成本、生态和维护性的偏好
- [ ] 验证新会话能否复用 Skill 和偏好

## 暂不处理

- [ ] Web UI
- [ ] 用户登录与权限系统
- [ ] 数据库和多人协作
- [ ] PDF、图片等多模态输入
- [ ] 自动部署和云端托管
- [ ] 同时运行多个调研任务

以上事项只有在核心学习目标完成，或新的用户故事明确要求时，才进入 Sprint Backlog。

## Sprint 0 完成标准

Sprint 0 只有在以下条件全部满足后才可标记完成：

- [ ] 新环境可根据 README 完成安装
- [ ] 一条命令可以启动 TechScout
- [ ] 固定验收问题能够完整执行
- [ ] 本地成功生成符合格式的报告
- [ ] 核心单元测试全部通过
- [ ] 静态检查全部通过
- [ ] 仓库中没有 API Key 和运行缓存
- [ ] 除 LLM Token 外没有任何付费服务或自动计费配置
- [ ] Review 与 Retrospective 已记录
